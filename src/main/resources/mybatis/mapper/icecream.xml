<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.parfait.icecreamupgraded.dao.IcecreamDao">

    <select id="selectProduct" resultType="com.parfait.icecreamupgraded.dto.Product">
        select * from product
    </select>

    <select id="selectTopping" resultType="com.parfait.icecreamupgraded.dto.Topping">
        select * from topping
    </select>

</mapper>